Build the project for production.

Steps:
1. `pnpm clean` — remove the dist/ directory
2. `pnpm build` — type-check and compile TypeScript to dist/

If the build fails, show the full error output. If it succeeds, run `ls dist/` and show the output files.
